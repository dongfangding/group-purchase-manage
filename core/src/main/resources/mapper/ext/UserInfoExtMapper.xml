<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ddf.group.purchase.core.mapper.UserInfoMapper">
  <select id="selectByVerifiedEmail" resultMap="BaseResultMap">
    select <include refid="Base_Column_List" />
    from user_info
    where email = #{email} and email_verified = true
  </select>

  <select id="selectByBuildingAndRoomNo" resultMap="BaseResultMap">
    select <include refid="Base_Column_List" />
    from user_info
    where community_id = #{communityId}
      and building_no = #{buildingNo}
      and room_no = #{roomNo}
  </select>
</mapper>
